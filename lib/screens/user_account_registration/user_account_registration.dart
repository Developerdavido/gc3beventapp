import 'package:flutter/material.dart';

class UserAccountRegistration extends StatelessWidget {
  const UserAccountRegistration({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
