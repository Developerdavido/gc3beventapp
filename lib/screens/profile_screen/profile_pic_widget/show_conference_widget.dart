import 'package:flutter/material.dart';

class ShowConferenceWidget extends StatelessWidget {
  const ShowConferenceWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
