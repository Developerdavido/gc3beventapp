import 'package:flutter/material.dart';

class JoinedConferenceAndMeeting extends StatelessWidget {
  const JoinedConferenceAndMeeting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Column(
          children: [

          ],
        ),
      ),
    );
  }
}
